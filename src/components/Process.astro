---
const steps = [
  {
    number: '01',
    title: 'Discovery',
    description: 'Deep dive into your business, audience, and goals.',
    icon: `<svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
    </svg>`
  },
  {
    number: '02',
    title: 'Strategy',
    description: 'Custom roadmap tailored to your objectives.',
    icon: `<svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
    </svg>`
  },
  {
    number: '03',
    title: 'Execute',
    description: 'Bring strategy to life with precision and creativity.',
    icon: `<svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
    </svg>`
  },
  {
    number: '04',
    title: 'Optimize',
    description: 'Continuously refine to maximize your ROI.',
    icon: `<svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15" />
    </svg>`
  }
]
---

<section id="process" class="min-h-screen flex items-center py-12 px-6 bg-white relative overflow-hidden">
  <div class="max-w-7xl mx-auto w-full relative">
    <!-- Section Header -->
    <div class="text-center mb-8" data-animate="animate-fade-in-up">
      <span class="inline-block text-primary-500 font-semibold text-sm uppercase tracking-wider mb-2">
        Our Process
      </span>
      <h2 class="text-3xl md:text-4xl font-display font-bold text-dark-800 mb-3">
        How We Deliver <span class="gradient-text">Results</span>
      </h2>
      <p class="text-dark-500 max-w-2xl mx-auto">
        A proven framework that transforms your marketing into a predictable growth engine.
      </p>
    </div>

    <!-- Process Steps - Horizontal -->
    <div class="relative mb-8">
      <!-- Connection line (desktop) -->
      <div class="hidden lg:block absolute top-16 left-[10%] right-[10%] h-0.5 bg-gradient-to-r from-primary-200 via-primary-400 to-primary-200"></div>

      <div class="grid grid-cols-2 lg:grid-cols-4 gap-4 lg:gap-6">
        {steps.map((step, index) => (
          <div 
            class="relative text-center"
            data-animate="animate-fade-in-up"
            style={`animation-delay: ${index * 0.1}s`}
          >
            <!-- Number badge -->
            <div class="relative inline-flex mb-4">
              <div class="w-12 h-12 bg-primary-500 rounded-xl flex items-center justify-center text-white font-display font-bold text-lg mx-auto">
                {step.number}
              </div>
            </div>

            <!-- Content -->
            <h3 class="text-lg font-display font-bold text-dark-800 mb-2">
              {step.title}
            </h3>
            <p class="text-dark-500 text-sm">
              {step.description}
            </p>
          </div>
        ))}
      </div>
    </div>

    <!-- CTA -->
    <div class="text-center" data-animate="animate-fade-in-up">
      <a href="#contact" class="btn-primary">
        Book Your Free Strategy Call
        <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
        </svg>
      </a>
    </div>
  </div>
</section>
