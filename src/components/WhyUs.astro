---
const features = [
  {
    title: 'Results-Obsessed',
    description: 'Every decision we make is backed by data and measured by revenue impact.',
    icon: '📈'
  },
  {
    title: 'Founder-Led',
    description: 'Work directly with senior strategists, not junior account managers.',
    icon: '🎯'
  },
  {
    title: 'Speed to Market',
    description: 'Launch campaigns in days, not months. We move as fast as you do.',
    icon: '⚡'
  },
  {
    title: 'Full Transparency',
    description: 'Real-time dashboards and weekly reports. No smoke, no mirrors.',
    icon: '🔍'
  }
]
---

<section id="about" class="min-h-screen flex items-center py-20 px-6 bg-dark-900 relative overflow-hidden">
  <!-- Background elements -->
  <div class="absolute top-0 right-0 w-1/2 h-full bg-gradient-to-l from-primary-500/5 to-transparent"></div>
  <div class="absolute bottom-0 left-0 w-96 h-96 bg-primary-500/10 rounded-full blur-[128px]"></div>

  <div class="max-w-7xl mx-auto w-full relative">
    <div class="grid lg:grid-cols-2 gap-12 lg:gap-20 items-center">
      
      <!-- Left - Big statement -->
      <div>
        <span class="text-primary-400 font-semibold text-sm uppercase tracking-wider mb-4 block">
          Why Jraft
        </span>
        <h2 class="text-3xl lg:text-5xl font-display font-bold text-white leading-tight mb-6">
          Marketing That
          <span class="gradient-text block">Actually Works</span>
        </h2>
        <p class="text-dark-300 text-lg mb-8 leading-relaxed">
          We're not here to win awards. We're here to make you money. 
          Our strategies are built on what converts, not what's trendy.
        </p>

        <!-- Big stats -->
        <div class="flex gap-8">
          <div>
            <div class="text-4xl lg:text-5xl font-display font-bold text-primary-400">10+</div>
            <div class="text-dark-400 text-sm mt-1">Years Experience</div>
          </div>
          <div>
            <div class="text-4xl lg:text-5xl font-display font-bold text-primary-400">150+</div>
            <div class="text-dark-400 text-sm mt-1">Projects Delivered</div>
          </div>
        </div>
      </div>

      <!-- Right - Feature cards -->
      <div class="grid grid-cols-2 gap-4">
        {features.map((feature, index) => (
          <div 
            class="glass-card rounded-2xl p-5 hover:border-primary-500/30 transition-all duration-300"
            data-animate="animate-fade-in-up"
            style={`animation-delay: ${index * 0.1}s`}
          >
            <span class="text-2xl mb-3 block">{feature.icon}</span>
            <h3 class="text-white font-semibold mb-2">{feature.title}</h3>
            <p class="text-dark-400 text-sm leading-relaxed">{feature.description}</p>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>
