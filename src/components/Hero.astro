---
interface Props {
  title: string;
  subtitle: string;
  backgroundImage: string;
  heroLabel?: string;
  heroClass?: string;
  buttons?: { text: string; href: string; class: string }[];
}

const { title, subtitle, backgroundImage, heroLabel, heroClass = '', buttons = [] } = Astro.props;
---
<section class={`hero ${heroClass}`} aria-label="Page introduction">
  <div class="hero-bg" aria-hidden="true"><img src={backgroundImage} alt="" fetchpriority="high" decoding="async" class="hero-bg-img"></div>
  <div class="container">
    {heroLabel && <span class="hero-label">{heroLabel}</span>}
    <h1 set:html={title} />
    <p>{subtitle}</p>
    {buttons.length > 0 && (
      <div class="hero-buttons">
        {buttons.map(btn => (
          <a href={btn.href} class={btn.class}>{btn.text}</a>
        ))}
      </div>
    )}
  </div>
</section>
